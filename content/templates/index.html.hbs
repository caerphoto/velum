<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="/assets/style.css">
  </head>
  <body>
    <section id="content">
      <h1>{{title}}</h1>
      {{#each articles}}
        <h2><a href="{{this.route}}">{{this.title}}</a></h2>
      {{/each}}
    </section>
  </body>
</html>
