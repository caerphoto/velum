<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{title}}</h1>
    <section id="content">
      {{#each articles}}
        <h2><a href="{{this.route}}">{{this.title}}</a></h2>
      {{/each}}
    </section>
  </body>
</html>
