<div id="theme-selector-box" class="logout link-box">
  <label for="theme-selector">Theme:</label>
  <select id="theme-selector">
    {{#each themes}}
      <option value="{{filename}}">{{name}}</option>
    {{/each}}
  </select>
</div>
